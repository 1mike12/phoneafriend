{
  "scripts": {
    "refresh": "knex migrate:rollback && knex migrate:latest && node ./seed/dev"
  },
  "dependencies": {
    "socket.io": "^2.0.3",
    "socket.io-client": "^2.0.3",
    "uuid": "^3.1.0",
    "ws": "^3.2.0"
  }
}
